# Linux-command-web-interface

---

```sh
$ source myenv/bin/activate
# $ python -m pip install --upgrade pip
# $ python -m pip install flask python-dotenv
# $ python -m pip freeze > requirements.txt
$ python -m pip install -r requirements.txt

$ python webapp.py
```
